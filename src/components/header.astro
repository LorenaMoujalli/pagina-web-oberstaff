---
import logo from "../assets/logo.png";
import iconoHam from "../assets/icono-hamburguesa.png";
import iconoConv from "../assets/icono-contacto.png";
---

<header class="header">
  <div class="container">
    <div class="header-content">
      <a href="#" class="logo">
        <img src={logo.src} alt="Logo" /> 
      </a> 

      <nav class="nav-links">
        <a href="#que-hacemos">¿Qué hacemos?</a>
        <a href="#como-funciona">¿Cómo funciona?</a>
        <a href="#testimonios">Testimonios</a>
        <a href="#preguntas-frecuentes">Preguntas frecuentes</a>
        <a href="#contacto">Contacto</a>
        <a href="#">Blog</a>
        <a href="#">¿Buscas trabajo?</a>
      </nav>
 
      <!-- Botón contacto -->
      <button id="openContactBtn" class="menu-btn contact-btn" aria-label="Abrir contacto">
        <img src={iconoConv.src} alt="Contacto" />
      </button>

      <!-- Botón hamburguesa -->
      <button id="openMenuBtn" class="menu-btn ham-btn" aria-label="Abrir menú">
        <img src={iconoHam.src} alt="Abrir menú" />
      </button>
    </div>
  </div>
</header>

<!--  MODAL MENÚ -->
<div id="menuModal" class="menu-modal">
  <div class="modal-backdrop" id="modalBackdropMenu"></div>
  <div class="modal-content">
    <div class="modal-header">
      <img src={logo.src} alt="Logo" class="modal-logo" />
      <button id="closeMenuBtn" class="close-btn" aria-label="Cerrar">×</button>
    </div>

    <nav class="modal-links">
      <a href="#que-hacemos">¿Qué hacemos?</a>
      <a href="#como-funciona">¿Cómo funciona?</a>
      <a href="#testimonios">Testimonios</a>
      <a href="#preguntas-frecuentes">Preguntas frecuentes</a>
      <a href="#contacto">Contacto</a>
      <a href="#">Blog</a>
      <a href="#">¿Buscas trabajo?</a>
    </nav>
  </div>
</div>

<!--MODAL CONTACTO -->
<div id="contactModal" class="menu-modal">
  <div class="modal-backdrop" id="modalBackdropContact"></div>
  <div class="modal-content contact-content">
    <div class="modal-header">
      <img src="" alt="" class="modal-logo" />
      <button id="closeContactBtn" class="close-btn" aria-label="Cerrar">×</button>
    </div>
 
    <h2>Envíanos <span class="mensaje">un mensaje</span></h2>
    <p>Envíanos tus dudas y te responderemos lo más pronto posible.</p>

    <form class="contact-form">
      <input type="text" placeholder="Nombre y apellido" required />
      <input type="email" placeholder="Email" required />

      <div class="phone-group">
        <input type="text" placeholder="+XX" class="country-code" required />
        <input type="text" placeholder="Teléfono" class="phone-number" required />
      </div>

      <textarea placeholder="Mensaje" rows="4" required></textarea>

      <button type="submit" class="btn-send">ENVIAR MENSAJE</button>
    </form>
  </div>
</div>

<script src="./scripts/menu.js" type="module"></script>




<style>
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
  @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap'); 

.header {
  background: #f4f4f4;
  padding: 10px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.05);
  position: fixed;   
  top: 0;            
  left: 0;
  width: 100%;       
  z-index: 1000;  
}


.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1rem;
  box-sizing: border-box;
}

.header-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: nowrap;
} 

.logo img {
  max-height: 40px;
} 


.nav-links {
  display: flex;
  gap: 20px;
  flex-wrap: wrap;         
  justify-content: center; 
}

.nav-links a {
  text-decoration: none;
  color: #333;
  white-space: nowrap;     
  font-size: 0.90rem;
   font-family: 'Poppins', sans-serif; 
}

.nav-links a:hover{
  color: #141414; 
  font-size: 0.92rem; 
}

.menu-btn { 
  background: none;
  border: none; 
  cursor: pointer;
  flex-shrink: 0; 
} 

.menu-btn img {
  width: 25px;
  height: 25px;
}

.ham-btn { display: none; }

.mensaje{
  color: #ff65c3;
}

                              /* MODAL MENÚ  */

.menu-modal {
  display: none;
  position: fixed;
  inset: 0;
  z-index: 9999;
}

.menu-modal.show {
  display: flex;
  align-items: center;
  justify-content: center;
}

.modal-backdrop {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
}

.modal-content {
  position: relative;
  background: white;
  border-radius: 20px;
  padding: 2rem;
  width: 90%;
  max-width: 350px;
  z-index: 1;
  text-align: center;
  box-shadow: 0 4px 20px rgba(0,0,0,0.2);
  animation: fadeIn 0.3s ease;
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.modal-logo {
  max-height: 35px;
}

.close-btn {
  background: none;
  border: none;
  font-size: 1.8rem;
  cursor: pointer;
  color: #000;
}

.modal-links {
  display: flex;
  flex-direction: column;
  margin-top: 1.5rem;
  
}

.modal-links a {
  text-decoration: none;
  color: white;
  padding: 0.8rem;
  border-radius: 10px;
  margin-bottom: 0.6rem;
  font-weight: 500;
}

.modal-links a {
  background-color: #fff;  
  border: 2px solid;      
  padding: 0.8rem;
  border-radius: 10px;
  margin-bottom: 0.6rem;
  font-weight: bold;
  transition: all 0.3s ease;
  font-family: 'Poppins', sans-serif; 
}

.modal-links a:nth-child(odd) {
  border-color: #333;   
   color: #333;
}

.modal-links a:nth-child(even) {
  border-color: #333;      
   color: #333;
}

.modal-links a:hover {
  background-color: currentColor;
  color: #fff;
}

                                           /*  CONTACTO  */

.contact-content {
  text-align: left;
}

.contact-content h2 {
  font-size: 1.4rem;
  margin-top: 1rem;
  text-align: center;
  font-family: 'Inter', sans-serif;
  font-weight: bold;
  margin-bottom: 10px;
}

.contact-content p {
  font-size: 0.9rem;
  color: #333;
  margin-bottom: 1.2rem;
  text-align: center;
  font-family: 'Inter', sans-serif;
  font-weight: bold;
}

.contact-form {
  display: flex;
  flex-direction: column;
  gap: 0.8rem;
}

.contact-form input,
.contact-form textarea {
  padding: 0.6rem;
  border: 1px solid #ccc;
  border-radius: 8px;
  font-size: 0.9rem;
  border: 2px solid #ff65c3;
}

.phone-group {
  display: flex;
  gap: 0.5rem;
}

.country-code {
  width: 25%;
}

.phone-number {
  width: 75%;
}

.btn-send {
  font-family: 'Inter', sans-serif;
  font-weight: bold;
  background: #ff65c3;
  color: white;
  border: none;
  padding: 0.8rem;
  border-radius: 20px; 
  cursor: pointer;
  font-weight: 600;
  margin-top: 0.5rem;
  transition: background 0.3s ease;
}

.btn-send:hover {
  background: #ff48b4;
}

.contact-modal-content input,
.contact-modal-content textarea {
  border: 2px solid #ff65c3;
}


                                           /*RESPONSIVE*/ 

@media (max-width: 768px) {
  .nav-links {
     display: none; 
    }

  .ham-btn {
     display: block; 
    }

 
  html, body {
    overflow-x: hidden;
  }

  .modal-links a{
    font-size: 0.85rem;  
  }
}


@media (min-width: 769px) {
  .header-content {
    justify-content: space-between;
    align-items: center;
  }

 .contact-btn {
    width: auto;           /* ancho automático para texto */
    height: auto;
    padding: 10px;
    background: #FF6ACD;
    color: white;
    font-family: 'Poppins', sans-serif;
    font-weight: 500;
    font-size: 13px;
    border-radius: 20px;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    
  } 

  .contact-btn:hover {
     background: #f85ac3;
     
  } 

  .contact-btn img {
    display: none;         /* ocultamos el icono en desktop */
  }

  .contact-btn::after {
    content: 'Contacto';  /* agregamos el texto dentro del botón */
  }


}

</style>
 