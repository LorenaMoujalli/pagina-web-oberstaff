---
import { Image } from 'astro:assets';
const { img, title, text, class: cardClass } = Astro.props;
---

<div class={cardClass}>
  <Image
    src={img}
    alt={title}
    width={400}  
    height={300}
    format="webp"
    class="img-replace"
  />
  <h3>{title}</h3>
  <p>{text}</p>
</div>
